name: Build Windows EXE

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure
        run: cmake -S . -B build -A x64

      - name: Build Release
        run: cmake --build build --config Release --parallel

      - name: Verify EXE
        shell: pwsh
        run: |
          if (!(Test-Path 'build\Release\NeonDebugger.exe')) { throw 'NeonDebugger.exe was not produced' }

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: NeonDebugger-Windows-x64
          path: build\Release\NeonDebugger.exe
          if-no-files-found: error

      - name: Create GitHub Release with direct EXE
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build\Release\NeonDebugger.exe
          generate_release_notes: true
          name: NeonDebugger ${{ github.ref_name }}
